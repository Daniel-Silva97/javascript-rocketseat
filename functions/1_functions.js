// Criar um aplicativo de frases motivacionais

//Declarando uma função - Function declaration
function createPhrases(){
    console.log('Estudar é muito bom')
    console.log('Paciência e Persistência')
    console.log('Revisão é mãe do aprendizado')
}

//Executar a função
//Execute, run, call, invoke the function

createPhrases()
createPhrases()
createPhrases()
createPhrases()
createPhrases()


/* 
Quando criamos a função mais acima, armazenamos os dados na memória e podemos chamá-los a qualquer momento com o comando acima, o programa com código no formato atual, rodaria na seguinte sequencia:

    * Cria a função createPhrases com os valores de console.log atribuidos a ela
    * Chamamos a função, podendo fazer isso diversas vezes com o comando acima.
    * O programa identifica que a função existe e roda tudo o que há dentro dela quantas vezes for necessária
    * Após executar a função quantas vezes for necessária, encerrará com o console.log abaixo.
*/
console.log('Fim do programa')