// Operador do String (String operator)

// Comparison (Comparação)
console.log('a' == 'b')
console.log('a' == 'a')

// Concatenation (Concatenação)
console.log('alpha' + 'bet')

// Retorna a união de duas strings
let alpha = 'alpha'
console.log(alpha + 'bet')
