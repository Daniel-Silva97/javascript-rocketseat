//Manipulando Arrays

// Transformar uma cadeia de caracteres(String) em elementos de um array

//Criando a cadeia de caracteres
let word = "Manipulação"

//Transformando em Array
console.log(Array.from(word))

//Chamando o objeto Array, podemos usar o método from que converte uma string em Array, basta informar dentro do parenteses o que deseja converter.